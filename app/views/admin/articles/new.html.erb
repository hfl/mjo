<div class="card mb-3">
  <div class="card-header">
    <i class="fas fa-file"></i>
    新增文章
  </div>
  <div class="card-body">
    <%= render 'form', article: @article %>
    <%= link_to 'Back', admin_journal_issue_articles_path(@article.issue.journal, @article.issue, @article) %>
  </div>
</div>
