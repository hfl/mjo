<%= form_with(model: journal, url: [:admin, journal], local: true) do |form| %>
  <% if journal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(journal.errors.count, "error") %> prohibited this journal from being saved:</h2>

      <ul>
      <% journal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group row">
    <%= form.label :name, class: "col-md-2 col-form-label" %>
    <div class="col-md-10">
      <%= form.text_field :name, class: "form-control" %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :about, class: "col-md-2 col-form-label" %>
    <div class="col-md-10">
      <%= form.text_area :about, class: "form-control" %>
    </div>
  </div>

  <div class="from-group row">
    <%= form.label :issn, class: "col-md-2 col-form-label" %>
    <div class="col-md-10">
      <%= form.text_field :issn, class: "form-control" %>
    </div>
  </div>

  <div class="form-group row">
    <div class="offset-md-2 col-md-10">
      <br>
      <%= form.submit class: "btn btn-primary mb-2" %>
    </div>
  </div>
<% end %>
